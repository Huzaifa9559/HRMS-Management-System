services:
  # Test MySQL Database Service
  mysql-test:
    image: mysql:8.0
    container_name: hrms-mysql-test
    restart: unless-stopped
    ports:
      - "3307:3306"  # Different port to avoid conflict with dev database
    environment:
      - MYSQL_ROOT_PASSWORD=testpassword
      - MYSQL_DATABASE=test_db
      # Note: MYSQL_USER and MYSQL_PASSWORD are for regular users, not root
      # We use root user directly with MYSQL_ROOT_PASSWORD
    command: --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-ptestpassword"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - test-network

networks:
  test-network:
    driver: bridge

